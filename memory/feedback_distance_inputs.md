# 寸法入力は Blender の単位型を使う

- 寸法・隙間は単なる数値と `(mm)` ラベルではなく、`FloatProperty(subtype="DISTANCE", unit="LENGTH")` で入力する。
- シーンの単位設定に従う表示と単位付き入力を使えるようにする。角度なども適切な subtype を使う。
- 既存の mm 保存プロパティがある場合は距離型の表示プロパティを介し、保存値との互換性を保つ。
